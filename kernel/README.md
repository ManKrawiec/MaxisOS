# Kernel

Use the scripts in this directory to configure, build, and install the Linux kernel.

- `configure-kernel.sh` runs menuconfig
- `build-kernel.sh` builds the kernel and modules
- `install-kernel.sh` installs into a rootfs
- `update-kernel.sh` downloads a new kernel tarball
